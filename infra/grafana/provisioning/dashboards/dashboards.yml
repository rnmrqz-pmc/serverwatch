apiVersion: 1

providers:
  - name: ServerWatch
    orgId: 1
    folder: ServerWatch
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards
